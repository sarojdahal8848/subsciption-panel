import { ICustomCard } from './CustomCard';

const card: ICustomCard = {
  title: 'Total User',
  value: '2000',
  bgColor: 'blue',
  textColor: 'White',
};

export const mockCustomCardProps = {
  card,
};
